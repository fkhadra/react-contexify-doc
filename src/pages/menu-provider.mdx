import Playground from "../components/Playground";
import { menuProvider } from "../data/playground";

# Menu Provider

By default the `MenuProvider` component wrap the given children with a `div` element. The component expose 2 useful props to override
this behavior. `component` and `render` let's see how to use them.

<Playground code={menuProvider} />
