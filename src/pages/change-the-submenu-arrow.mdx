import Playground from "../components/Playground";
import { customArrow } from "../data/playground";

# Change Submenu arrow

The `Submenu` component expose an `arrow` props. You can provide any valid ReactNode. Let's use a unicorn!

<Playground code={customArrow} />