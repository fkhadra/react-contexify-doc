import Playground from '../components/Playground';
import { gettingStarted } from '../data/playground';

# Getting Started

## Installation

Install the library with a package manager of your choice

```sh
$ yarn add react-contexify
```

```sh
$ npm install --save react-contexify
```

Import the needed components and the stylesheet

```js
import { ContextMenu, Item, Separator, Submenu, MenuProvider } from 'react-contexify';
import 'react-contexify/dist/ReactContexify.min.css';
```

## Usage

<Playground code={gettingStarted}/>
