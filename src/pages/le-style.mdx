import Playground from '../components/Playground';
import { leStyle } from "../data/playground";

# Le style !

React-contexify comes with built-in theme and built-in entrance animation. Check the demo to see them in action. View the api 
for more details.

```js
import { theme, animation } from 'react-contexify';
```

<Playground code={leStyle} />
