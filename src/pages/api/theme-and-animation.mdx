# Theme and Animation

Available theme: `dark`, `light`

Available animation: `zoom`, `pop`, `flip`, `fade`

```js
import { Menu, Item, theme, animation } from 'react-contexify';

<Menu id="foo" theme={theme.dark} animation={animation.zoom}>
  <Item>Copy</Item>
  <Item>Paste</Item>
</Menu>
```