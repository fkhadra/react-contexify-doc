import PropTable from '../../components/PropTable';
import { provider } from '../../data/component-props';

# MenuProvider

<PropTable of={provider} />

```js
import { MenuProvider } from "react-contexify";

<MenuProvider id="foo" component="span" event="onClick">
  <MyComponent />
</MenuProvider>
```
